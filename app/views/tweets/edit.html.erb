<h1>おさんぽ記録を編集</h1>

<% if @tweet.images.attached? %>
  <p>現在の写真:</p>
  <div class="edit-images-grid">
    <% @tweet.images.each do |image| %>
      <div class="edit-image-block">
        <%= image_tag image, class: "edit-preview-image" %>
        <div class="edit-image-actions">
          <%= button_to "↺ 左に回転", rotate_image_tweet_path(@tweet),
                params: { image_id: image.id, direction: 'ccw' },
                class: "btn-rotate", data: { turbo: false } %>
          <%= button_to "↻ 右に回転", rotate_image_tweet_path(@tweet),
                params: { image_id: image.id, direction: 'cw' },
                class: "btn-rotate", data: { turbo: false } %>
          <%= button_to "削除", destroy_image_tweet_path(@tweet),
                params: { image_id: image.id },
                method: :delete,
                class: "btn-delete-image",
                data: { turbo: false, confirm: "この写真を削除しますか？" } %>
        </div>
      </div>
    <% end %>
  </div>
<% end %>

<%= form_with model: @tweet, local: true do |f| %>

  <!-- Subject -->
  <div class="field">
    <%= f.label :subject, "タイトル" %>
    <%= f.text_field :subject %>
  </div>

  <!-- Date -->
  <div class="field">
    <%= f.label :activity_date, "日付" %>
    <%= f.date_field :activity_date %>
  </div>

  <!-- Text -->
  <div class="field">
    <%= f.label :text, "内容" %>
    <%= f.text_area :text %>
  </div>

  <!-- Images -->
  <div class="field">
    <%= f.label :images, "写真（追加する場合・10枚まで）" %><br>
    <div data-controller="image-preview">
      <%= f.file_field :images, name: "tweet[images][]", accept: "image/*",
            multiple: true,
            data: { image_preview_target: "input", action: "change->image-preview#fileSelected" } %>
      <div data-image-preview-target="container" class="upload-preview-container"></div>
    </div>
  </div>

  <%= f.submit "更新する", class: "btn-edit" %>

<% end %>

<br>
<%= link_to "戻る", tweet_path(@tweet) %>
